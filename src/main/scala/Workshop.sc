object Workshop {

  val name = "My name"

  name

  val exp = if ( 2 > 4 ) "yes" else "no"
  val price = 20

  def max (x:Int, y:Int ):Int = {
    val ten = if (x > y) x else y
    return ten
  }

  max(10,33)
}